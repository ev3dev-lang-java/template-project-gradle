
//Configuration
project.ext {

    //Main program
    mainClass = "example.robotics.ev3.actuator.LEDPatternsExample"

    //Brick
    brickHost = "192.168.2.3"
    brickUser = "robot"
    brickPassword = "maker"

    //JVM
    //jvmFlags = "-verbose:class"
    jvmFlags = "-Xshare:on"
    //jvmFlags = "-Dcom.sun.management.jmxremote=true -Dcom.sun.management.jmxremote.port=7091 -Dcom.sun.management.jmxremote.authenticate=false -Dcom.sun.management.jmxremote.ssl=false"

    //Directory paths
    externalJarsPath = "/home/robot/libs"
    userJarsPath = "/home/robot"

    prefix = "time brickrun -- "

    userJarName = { fat = true ->
    }

    userJarPath = { fat = true ->
        def suffix = fat ? "-all" : "";
        return "${project.userJarsPath}/${rootProject.name}-${version}${suffix}.jar";
    }
}
